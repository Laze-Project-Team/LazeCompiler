関数: 表示(実数: x) => () = js読み込み("console", "log");
関数: 実行() => (){
    整数: a = 5;
    実数: b = 10.0;
    もし(a == 5)ならば{
        表示(6583);
        整数: c = a * b;
        表示(c);
    }
    でなければ{
        表示(0);
        実数: c = a / b;
        表示(c);
    }
}