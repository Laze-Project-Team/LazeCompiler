(実数 の x) を とり () を 返す 表示 という 関数 を ("console", "log") から 読み込む 。
() を とり () を 返す 実行 という 関数 を おく 。{
    5 を 持つ a という 整数 を おく 。
    10.0 を 持つ b という 実数 を おく 。
    もし a は 5 ならば{
        6583 表示 。
        a * b を 持つ c という 整数 を おく 。
        c 表示 。
    }
    でなければ{
        0 表示 。
        a / b を 持つ c という 整数 を おく 。
        c 表示 。
    }
}